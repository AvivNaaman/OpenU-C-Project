/* Contains function prototypes of the very base of the assembler (mostly on files).  */

/* Returns the line number that is being processed. */
long get_curr_line();

/* Returns the name of the file that is being processed */
char *get_curr_filename();
